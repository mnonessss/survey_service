map $http_accept $want_html {
    default 0;
    "~*text/html" 1;
}

server {
    listen 80;
    root /usr/share/nginx/html;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }

    location /api/ {
        proxy_pass http://backend:8000;
    }
    location /auth/login {
        proxy_pass http://backend:8000;
    }
    location /auth/ {
        if ($want_html = 1) {
            rewrite ^ /index.html last;
        }
        proxy_pass http://backend:8000;
    }
    location /surveys/ {
        if ($want_html = 1) {
            rewrite ^ /index.html last;
        }
        proxy_pass http://backend:8000;
    }
    location /integrations/ {
        proxy_pass http://backend:8000;
    }
    location /public/ {
        proxy_pass http://backend:8000;
    }
    location /uploads/ {
        proxy_pass http://backend:8000;
    }
    location /health {
        proxy_pass http://backend:8000;
    }
    location /docs {
        proxy_pass http://backend:8000;
    }
    location /openapi.json {
        proxy_pass http://backend:8000;
    }
}
